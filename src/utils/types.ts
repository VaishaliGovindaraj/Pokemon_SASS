export type HomeGridProps ={
    title: string,
    image: string,
    position: string,
    bgColor: string
}

export type PokemonType = {
    id: number,
    name: string,
    image: string,
    types: string [],
 
}